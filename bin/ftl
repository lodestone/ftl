#!/usr/bin/env ruby
$LOAD_PATH << 'lib'
require 'optparse'
require 'ftl'

options = {}
optparse = OptionParser.new do |opts|

  opts.on('-h', '--help', "Display this message") do
    Ftl.help
    exit
  end

  opts.on('-d', '--headers FILE', Array, "provide custom config file") do |headers|
    options[:headers] = headers
  end

  opts.on('-c', '--config FILE', "provide custom config file") do |file|
    options[:config] = file
  end

end

optparse.parse!
Ftl::Client.new(ARGV, options)

